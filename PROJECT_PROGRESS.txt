Project Progress
███████████░ 92%

Completed:
✓ User-reported issues triage complete (History delete, dashboard UX, visibility, logs, duplicate-filter trace)
✓ Root-cause identified for History delete failure (terminal-state mismatch between frontend and backend checks)
✓ History delete logic fixed (backend terminal evaluation now publish-state aware)
✓ Approvals history delete button now respects backend `can_delete` state
✓ Project visibility hardened: admins/sub-admins now see/access only own or explicitly assigned projects
✓ Dashboard clarity improved (mode badges labeled as status + Claude billing CTA)
✓ Header/sidebar UX cleanup (logo now routes to Dashboard, duplicate role text removed)
✓ Duplicate-filter observability improved with reason breakdown in pipeline step log
✓ Project-scoped logs implemented (`app_id`), Logs page now supports scoped clear action
✓ Added project-level AI key override support (Claude/OpenAI per project + global fallback)
✓ Sub-admin assignment table now labels assigned projects as `Assigned by Admin`
✓ Targeted regression suite passed (`48 passed`)
✓ Full backend regression suite passed (`129 passed`)
✓ Frontend production build passed (`npm run build`)

Remaining:
□ Cross-role assignment workflow (sub-admin ↔ admin assignment UI/API flow) with explicit API-source popup policy
□ True Claude wallet balance amount + `< $1` threshold alert (provider does not expose direct wallet balance in current integration)
